// These properties are like clientWidth/clientHeight, but they also
// include the scrolled out (hidden) parts:

// expand the element to the full content height
// element.style.height = `${element.scrollHeight}px`;
