// If there are no paddings, then clientWidth/Height is exactly the
// content area, inside the borders and the scrollbar (if any).

// So when there’s no padding we can use clientWidth/clientHeight to get
// the content area size.
